# Auto detect text files and perform LF normalization
* text=auto

# Batch files should use CRLF line endings (Windows standard)
*.bat text eol=crlf
*.cmd text eol=crlf

# Ensure batch files are treated as text and not binary
*.bat diff
*.cmd diff

