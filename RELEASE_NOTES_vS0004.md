# 股票記帳系統 vS0004 發行說明

## 版本信息
- **版本號**: vS0004
- **發布日期**: 2025年1月
- **編碼**: UTF-8

## 主要更新

### 重要修復

#### 批處理文件行尾符修復
- **問題修復**
  - 修復 GitHub Source code 壓縮包中批處理文件無法打開的問題
  - 文件現在使用 CRLF 行尾符（Windows 標準）
  - 添加 `.gitattributes` 文件確保所有批處理文件使用正確的行尾符

- **技術改進**
  - 創建 `.gitattributes` 文件
  - 設置 `*.bat` 和 `*.cmd` 文件強制使用 CRLF 行尾符
  - 確保批處理文件被正確識別為文本文件
  - 重新規範化所有批處理文件

### 包含的文件

#### Source code 壓縮包
GitHub 自動生成的 Source code 壓縮包現在包含：
- `start.bat` - 系統啟動腳本（使用 CRLF 行尾符）
- `stop.bat` - 系統停止腳本（使用 CRLF 行尾符）
- `.gitattributes` - Git 屬性配置文件
- 所有其他項目文件

## 技術細節

### Git 配置
- 添加 `.gitattributes` 文件
- 批處理文件行尾符規範化為 CRLF
- 確保跨平台兼容性

### 文件編碼
- 所有批處理文件使用 UTF-8 編碼（無BOM）
- 行尾符使用 CRLF（Windows 標準）

## 升級說明

### 從 vS0003 升級到 vS0004

#### 升級步驟
1. **下載新版本**
   - 從 GitHub Release 頁面下載 Source code 壓縮包
   - 或使用 `git pull` 獲取最新代碼

2. **解壓文件**
   - 解壓 Source code 壓縮包
   - 批處理文件現在可以在 Windows 上正常運行

3. **安裝依賴**（如需要）
   ```bash
   npm run install:all
   ```

4. **使用批處理文件**
   - 運行 `start.bat` 啟動系統
   - 運行 `stop.bat` 停止系統

### 注意事項
- **重要**：vS0004 的 Source code 壓縮包中的批處理文件可以正常使用
- 批處理文件現在使用正確的 CRLF 行尾符
- 所有批處理文件都可以在 Windows 上直接運行

## 已知問題
- 無已知問題

## 後續計劃
- 持續改進系統穩定性
- 優化用戶體驗
- 加強錯誤處理

## 文件清單

### 新增/更新文件
- `.gitattributes` - Git 屬性配置（新增）
- `start.bat` - 重新規範化行尾符
- `stop.bat` - 重新規範化行尾符

## 支持與反饋

如有問題或建議，請聯繫開發團隊。

---

**編碼說明**: 本文檔使用 UTF-8 編碼保存，確保中文文字正確顯示。

